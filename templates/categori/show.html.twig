{% extends 'log.html.twig' %}


{% block body %}
    <h1>Categori</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categori.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categori.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categori.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categori_index') }}">back to list</a>

    <a href="{{ path('app_categori_edit', {'id': categori.id}) }}">edit</a>

    {{ include('categori/_delete_form.html.twig') }}
    
{% endblock %}
